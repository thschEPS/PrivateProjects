pieceSetUp.forEach(function (entry) {
                      if (entry.position === grid[this.x][this.y]) {
                        _.withOut(pieceSetUp, entry);
                      }
                    });
